---
label: Slideshow Block
hide_body: true
fields:
- name: lefthand_text
  type: include
  config: {}
  template: lefthand-text
  label: Lefthand Text
- name: image
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: caption
    type: text
    config:
      required: false
    label: Caption
  config:
    min: 
    max: 
    labelField: 
  label: Slideshow Image
- name: automatic
  type: boolean
  label: Automatic
  description: Should the slideshow run automatically?
