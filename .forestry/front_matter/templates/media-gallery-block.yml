---
label: Media Gallery Block
hide_body: false
fields:
- name: media
  type: field_group_list
  fields:
  - name: image/video
    type: file
    config:
      maxSize: 64
    label: Image/Video
  - name: image_title
    type: text
    config:
      required: false
    label: Image Title
  config:
    min: 
    max: 
    labelField: 
  label: Media
